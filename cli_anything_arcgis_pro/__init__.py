"""arcgis-cli — an agent-friendly command-line wrapper around ArcGIS Pro's ArcPy.

Runs INSIDE ArcGIS Pro's conda python (arcgispro-py3). Every command supports
``--json`` for structured, agent-parseable output. The standout module is
``layout`` — professional cartographic export (layouts + Map Series / map books)
which is ArcGIS Pro's main advantage over QGIS.
"""

__version__ = "0.1.0"
